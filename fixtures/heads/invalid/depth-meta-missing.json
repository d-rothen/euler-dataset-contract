{
  "description": "A registered modality with required meta fields cannot omit meta entirely.",
  "expected_exception": "ValueError",
  "expected_error": "dataset_head.modality.meta is required for modality.key='depth'",
  "head": {
    "contract": {
      "kind": "dataset_head",
      "version": "1.0"
    },
    "dataset": {
      "id": "demo_depth",
      "name": "Demo Depth"
    },
    "modality": {
      "key": "depth"
    }
  }
}
